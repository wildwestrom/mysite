module.exports = {
    purge: false,
    darkMode: 'media', // or 'media' or 'class'
    theme: {},
    variants: {
        extend: {
            textColor: ['visited'],
        }
    },
    plugins: []
}
